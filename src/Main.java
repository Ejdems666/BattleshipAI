/**
 * Created by Ejdems on 06/12/2016.
 */
public class Main {
    public static void main(String[] args) {
//        ParityCalculator parityCalculator = new ParityCalculator(0);
//        MyShots myShots = new MyShots(10, 10);
//        HuntMode huntMode = new HuntMode(myShots,parityCalculator);
//        Fleet fleet = new SimpleFleet(new int[]{2,2,3,4,5});
//        for (int i = 0; i < 20; i++) {
//            myShots.setLastShot(huntMode.getShot(fleet));
//            myShots.registerHit(false);
//        }
////        myShots.setLastShot(huntMode.getShot(fleet));
////        myShots.registerHit(true);
////        TargetMode targetMode = new TargetMode(myShots);
////        for (int i = 0; i < 5; i++) {
////            Position shot = targetMode.getShot(fleet);
////            myShots.setLastShot(shot);
////            myShots.registerHit(false);
////            myShots.printGrid();
////        }
//        System.out.println("\n_____________________________________________________________________\n");
//        myShots = new MyShots(10, 10);
//        huntMode = new HuntMode(myShots,parityCalculator);
//        for (int i = 0; i < 20; i++) {
//            myShots.setLastShot(huntMode.getShot(fleet));
//            myShots.registerHit(false);
//        }
    }
}
